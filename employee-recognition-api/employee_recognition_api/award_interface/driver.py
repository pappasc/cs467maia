import os 
import logging
from builder import Builder
from interpreter import Interpreter

class Driver: 
    """Integrates builder, interpreter, and distributer classes
    """

    def __init__():
    	self.builder = Builder()
    	self.interpreter = Interpreter()
    #	self.distributer = Distributer()

    #def run():
    #	self.builder.get_image_from_bucket(signature_path)